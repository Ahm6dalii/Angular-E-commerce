import { UpperCaseStPipe } from './upper-case-st.pipe';

describe('UpperCaseStPipe', () => {
  it('create an instance', () => {
    const pipe = new UpperCaseStPipe();
    expect(pipe).toBeTruthy();
  });
});
