import { RangeLoopPipe } from './range-loop.pipe';

describe('RangeLoopPipe', () => {
  it('create an instance', () => {
    const pipe = new RangeLoopPipe();
    expect(pipe).toBeTruthy();
  });
});
