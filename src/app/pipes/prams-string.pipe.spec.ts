import { PramsStringPipe } from './prams-string.pipe';

describe('PramsStringPipe', () => {
  it('create an instance', () => {
    const pipe = new PramsStringPipe();
    expect(pipe).toBeTruthy();
  });
});
